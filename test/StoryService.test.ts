// @vitest-environment node

import {describe} from "@effect/vitest"


describe("Story", () => {
  /*
    it("tips", async () => {
      try {
        await RunnerServic
        const res = await tips();
        console.log(res)
      } catch (e) {
        expect(e).toBeInstanceOf(Error)
        if(e instanceof Error) console.log(e.message)
      }
    })
    it("set_language", async () => {
      try {
        const res = await setLanguage("Japanese");
        console.log(res)
      } catch (e) {
        expect(e).toBeInstanceOf(Error)
        if(e instanceof Error) console.log(e.message)
      }
    })
    it("set_traveler_info", async () => {
      try {
        const res = await setTravelerInfo("traveler's name is mi");
        console.log(res)
      } catch (e) {
        expect(e).toBeInstanceOf(Error)
        if(e instanceof Error) console.log(e.message)
      }
    })
    it("getCurrentLocation", async () => {
      try {
        const res = await getCurrentLocationInfo(false,false,true);
        console.log(JSON.stringify(res,undefined,2).slice(0,200))
      } catch (e) {
        expect(e).toBeInstanceOf(Error)
        if(e instanceof Error) console.log(e.message)
      }

    })
    it("setCurrentLocation", async () => {
      try {
        const res = await setCurrentLocation('横浜市桜木町');
        console.log(res)
        //  現状Effectのサービスはpromiseでの起動の間を渡れない感じか。。
        const res2 = await getCurrentLocationInfo(false,false,true);
        console.log(JSON.stringify(res2).slice(0,200))

        expect(res).toBeInstanceOf(Object)
      } catch (e) {
        console.log(e)
      }
    })
    it("getDestinationAddress", async () => {
      try {
        const res = await getDestinationAddress();
        console.log(res)
      } catch (e) {
        expect(e).toBeInstanceOf(Error)
        if(e instanceof Error) console.log(e.message)
      }

    })
    it("setDestinationAddress", async () => {
      try {
        const res = await setDestinationAddress("横浜市馬車道");
        console.log(res)
      } catch (e) {
        expect(e).toBeInstanceOf(Error)
        if(e instanceof Error) console.log(e.message)
      }

    })
    it("startJourney", async () => {
      try {
        const res = await startJourney();
        console.log(JSON.stringify(res,undefined,1).slice(0,200))
      } catch (e) {
        expect(e).toBeInstanceOf(Error)
        if(e instanceof Error) console.log(e.message)
      }

    })
  */

})
